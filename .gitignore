.claude/
__pycache__/
current_work/
logs/
venv/
.venv/
workflow/
workflow_backup/
workflow_backup_static/
요구사항/
# 로컬 백업 (복구/작업 기록용, git 공유 대상 아님)
backups/
mode_workflow/
asset/
asset_data/
style_lora_data/
pose_data/
# 캐릭터 메이커 단일 영속 세션 작업공간 (내 작업물, 배포 제외)
character_maker_data/
bot/
instance_lora/
current_mode_workflow/
auto_complete/
CLAUDE.md
AGENTS.md

# Key files (보안상 git에 포함하지 않음)
key/
요구사항/chansub_key_before_*.json

# Config (사용자별 설정)
config.json

# Updater config (경로/브랜치 등 사용자 정보)
updater_config.json

# Updater build artifacts
updater_dev/build/
updater_dev/dist/
updater_dev/*.spec

# Distribution patches (외부 드라이브 배포)
chain_presets/
customprompt/enhance_outfit_prompt_v4_storage/
customprompt/test_program/
patch_data.zip
patch_chain_presets.zip

# Knowledge graph outputs (rebuild with: graphify . or code-review-graph build)
graphify-out/
.code-review-graph/

# MCP config (로컬 환경설정)
.mcp.json

# Notification cache
notification_cache.json
# Built-in WD Tagger downloaded model (362MB ONNX, regenerate at runtime)
tagger_data/

# Runtime-downloaded model bundle
models/

# Runtime-downloaded / user-uploaded fonts (Noto Sans KR 자동 다운로드 + 업로드)
fonts/

# 임시 작업/검증 산출물 (스크립트 추출물, 스크린샷 등)
.work/

# 배포 실행기가 내려받는 프로젝트 전용 uv 바이너리
.tools/

# Windows 전용 ComfyUI 설치기가 만드는 독립 런타임(모델/.venv 포함)
comfy/

# 암호화 팩에서 푼 버전별 배포 워크플로우 원본
comfy_workflow_library/

# Runtime-downloaded cloudflared binary (Quick Tunnel 자동 다운로드)
.bin/

# Modal 원격 LoRA 삭제 예약 outbox(런타임 상태 파일)
modal_lora_delete_outbox.json

# Modal 원격 영상 삭제 예약 outbox(런타임 상태 파일)
modal_video_delete_outbox.json

# Vast SSH 키 (보안상 git에 포함하지 않음)
vast_ssh_key
vast_ssh_key.pub

# Vast 비용 보호/재시작 복구 상태(런타임 생성)
/runtime/vast_launch_guard.json
/runtime/vast_launch_guard.json.tmp
/runtime/vast_machine_favorites.json
