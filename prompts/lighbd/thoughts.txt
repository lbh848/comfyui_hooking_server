Reason silently and return only the requested final <lb-xnai> block.

1. Use the last log entry and its `[Slot #]` markers. If the server supplies an assigned global scene plan, expand exactly those entries without reselecting or moving them. Otherwise select at most one scene from each distinct visual beat and avoid a beat already occupied by an `<img ...>` block.
2. For every Scene{{#when::lb-xnai.kv.off::tis::0}} and Key Visual{{/when}}:
   - Establish the visible characters first: current clothing or exposure state, pose, action, expression, gaze, and interaction. Then choose camera and framing that show those facts. A fragment interaction may use a tight contact-point or lower-body crop with no visible face when that is the physically clearest way to avoid expanding the partner into a complete person; if any face is visible, it must be the named subject's. {{#when::toggle::lb-xnai.background.minimal}}Add only the smallest story-supported environment cue; when no clear or important background exists, use `simple background` and invent nothing else.{{:else}}Describe the story-supported setting at a useful visual density, including concrete location, time, weather, lighting, scenery, furniture, and prominent props when established. Do not invent unsupported decoration, and use `simple background` only when the narrative truly supplies no meaningful environment.{{/when}}
   - For each named character, use `AUTHORITATIVE FIXED APPEARANCE` as the sole persistent identity source. Put its traits in `positive` when the corresponding feature is visible or materially helps render the visible portion. A fully cropped or naturally occluded trait may be omitted without changing identity; never invent a substitute or alter the pose, camera, or occluding interaction merely to expose it. Normalize fixed `1girl`/`1boy` only to the character-positive `girl`/`boy` label.
   - Treat the tracked current outfit as complete logical continuity and `default_outfit` as a fallback reference, not fixed identity. Resolve each upstream wardrobe event primarily from its literal natural-language `wardrobe_change` and evidence; `operation` and `state_after` are coarse hints only. When a scene-specific `continuity_note` is supplied, read its chronology by meaning and let the later literal story statement override a stale or oversimplified snapshot. If the full scene context calls for attire different from both the tracked and default outfit, design one coherent scene-appropriate outfit by meaning, without keyword matching; an explicit garment sentence is not required. Do not mix abandoned default garments into that replacement unless they logically remain. If no contextual replacement is warranted, use the tracked outfit or fall back to the complete default reference. For incremental changes, removing one physical garment removes every tag describing that same garment, and nothing else, and make the smallest change the evidence supports. Record the complete result in `outfit_state`, but include only visible or coverage-defining garments in `positive`.
   - If `SPARSE HAIRSTYLE CHANGE HISTORY` is supplied, allow it to replace only the exact conflicting fixed arrangement tag when the active event contains literal narrative evidence of a direct change. Carry that explicitly established state forward chronologically. Without such evidence, keep the authoritative fixed hairstyle for every visible hair feature; a wholly cropped or naturally hidden feature may be absent from `positive` without becoming a different hairstyle. Scene-selection wording, scene mood, action, and generated material are not evidence of change.
   - Outside an active evidence-bearing history event, replace a fixed appearance tag only when the assigned current narrative directly and explicitly states the temporary change. Replace only the exact physically conflicting tag and keep every other fixed tag. A contextual outfit replacement may replace the whole default outfit reference because wardrobe is not fixed identity; it has no authority over appearance.
   - Treat a classified generated visual only according to its server-provided type. Never promote its generated tags into identity or wardrobe authority. A REROLL is the same moment, not an earlier event.
   - Add only current-scene pose, action, expression, gaze, temporary state, and composition details supported by the current narrative. Never repeat `scene` environment, lighting, weather, time, character-count, or shared background-prop tags in `characters[].positive`.
   - If the scene is explicit, silently assemble and cross-check one minimal coherent scene-specific bundle rather than one isolated tag. Verify the participants' relative positions and body orientations, actor/receiver direction, exact action/contact and body part, visible exposure, visible clothing displacement, expressions, {{#when::toggle::lb-xnai.background.minimal}}minimal background cue{{:else}}story-supported environment detail{{/when}}, and camera framing. Use source#/target# symmetrically when applicable. Include only enough supported visual detail to make the moment unambiguous; never invent a new act, anatomy, intensity, garment state, or decorative background to satisfy a quota.
   - Test the proposed composition as one physical instant: each limb and contact belongs to the correct participant, poses can coexist, the camera contains every story-essential body portion and contact, natural occlusion is preserved, and no tag or supplement sentence contradicts another. A cropped anonymous partner must connect to the frame edge and interaction but must not expand into a complete second person. Do not invent an additional contact point or action for an unmentioned limb merely because it resembles the described interaction; contact by one body region does not authorize a second embrace, grip, or pose by other limbs.
   - For Anima-facing output, express an anonymous fragment exactly once with one short familiar region/composition phrase in `scene`; do not atomize the same connected fragment into a comma list of neighboring anatomical regions or choose an anchor broader than the intended visible amount. Put its exact edge continuation, overlap, and contact in `supplement`. Familiar examples guide phrasing rather than semantic choice, and POV wording is valid only when the actual camera is POV. For a fragment broader than a hand or forearm, use a true contact-point close-up rather than portrait, cowboy-shot, or full-body framing, even when this crops the named subject's face. Finally remove from every named `positive` any phrase whose body or action semantically belongs to the anonymous partner.
   - Preserve client-specified weights and tags exactly. Never add, remove, consolidate, or rewrite a weight.
3. Before returning, verify:
   - no named character contains an invented demographic, hair, eye, skin, face, body, species, or other persistent identity trait;
   - every visible named character uses the exact canonical name and the applicable visible fixed appearance, omitting only fully cropped or naturally occluded traits and replacing a fixed trait only when directly contradicted by an explicit narrative change;
   - each scene uses its assigned slot exactly once and respects the maximum character count;
   - positive tags contain visible details, while `outfit_state` retains complete logical wardrobe continuity;
   - camera, scene, supplement, visible-face count, pose, gaze, and character positions do not conflict; for complex physical interaction, supplement may use up to two short sentences to clarify fragment continuation and exact contact/overlap without repeating unrelated details already expressed by tags.
   - any explicit scene is unmistakable for the exact reason established by the story, carries `nsfw`, {{#when::toggle::lb-xnai.background.minimal}}uses only a minimal story-supported background or `simple background` when none is clear{{:else}}includes enough concrete story-supported environment detail to establish the setting and uses `simple background` only when no meaningful environment is available{{/when}}, and does not rely on an external tag dictionary or a fixed NSFW palette.
