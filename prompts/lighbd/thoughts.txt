Reason silently and return only the requested final <lb-xnai> block.

1. Use the last log entry and its `[Slot #]` markers. If the server supplies an assigned global scene plan, expand exactly those entries without reselecting or moving them. Otherwise select at most one scene from each distinct visual beat and avoid a beat already occupied by an `<img ...>` block.
2. For every Scene{{#when::lb-xnai.kv.off::tis::0}} and Key Visual{{/when}}:
   - Establish the visible characters first: current clothing or exposure state, pose, action, expression, gaze, and interaction. Then choose camera and framing that show those facts. Add only the smallest story-supported environment cue; when no clear or important background exists, use `simple background` and invent nothing else.
   - For each named character, begin with the complete `AUTHORITATIVE FIXED APPEARANCE`. Do not creatively complete or reword persistent identity, except for the required count-label normalization from fixed `1girl`/`1boy` to character-positive `girl`/`boy`. Do not omit a fixed tag for framing, occlusion, brevity, or model preference.
   - Begin attire from the complete default/current base. Resolve each CALL1 wardrobe event primarily from its literal natural-language `wardrobe_change` and evidence; `operation` and `state_after` are coarse hints only. When a scene-specific `continuity_note` is supplied, read its chronology by meaning and let the later literal story statement override a stale or oversimplified snapshot. Apply three rules: (1) if prose is compatible with the default, the default tags win; (2) removing one physical garment removes every tag describing that same garment, and nothing else; (3) make the smallest change the evidence supports. Preserve every non-conflicting default detail in both `positive` and `outfit_state`.
   - If `SPARSE HAIRSTYLE CHANGE HISTORY` is supplied, resolve it chronologically against fixed appearance and carry the active arrangement forward into scenes that do not repeat the hairstyle. Change only the conflicting hairstyle-arrangement tag; history outranks current-scene prose, a generated reference is never hairstyle authority, and hair color, length, bangs, and every other fixed trait stay unchanged.
   - If the assigned current scene explicitly replaces a fixed/default feature, replace only the directly conflicting tag and keep every other authority tag. The server separately audits omissions and conflicts.
   - Treat a classified generated visual only according to its server-provided type. Never promote its generated tags into identity or wardrobe authority. A REROLL is the same moment, not an earlier event.
   - Add only current-scene pose, action, expression, gaze, temporary state, and composition details supported by the current narrative. Never repeat `scene` environment, lighting, weather, time, character-count, or shared background-prop tags in `characters[].positive`.
   - If the scene is explicit, silently assemble and cross-check a coherent scene-specific bundle rather than one isolated tag. Verify the participants' relative positions and body orientations, actor/receiver direction, exact action/contact and body part, visible exposure, clothing displacement and remaining garments, expressions, minimal background cue, and camera framing. Use source#/target# symmetrically when applicable. Include at least five complementary supported visual details, but never invent a new act, anatomy, intensity, or garment state to satisfy the count. Never invent decorative background detail to satisfy it either.
   - Test the proposed composition as one physical instant: each limb and contact belongs to the correct participant, poses can coexist, the camera contains every story-essential body part/contact/garment state, and no tag or supplement sentence contradicts another.
   - Preserve client-specified weights and tags exactly. Never add, remove, consolidate, or rewrite a weight.
3. Before returning, verify:
   - no named character contains an invented demographic, hair, eye, skin, face, body, species, or other persistent identity trait;
   - every visible named character uses the exact canonical name and all applicable fixed tags;
   - each scene uses its assigned slot exactly once and respects the maximum character count;
   - positive tags contain visible details, while `outfit_state` retains complete logical wardrobe continuity;
   - camera, scene, supplement, visible-face count, pose, gaze, and character positions do not conflict, and supplement does not repeat details already expressed by tags.
   - any explicit scene is unmistakable for the exact reason established by the story, carries `nsfw`, uses only a minimal story-supported background or `simple background` when none is clear, and does not rely on an external tag dictionary or a fixed NSFW palette.
