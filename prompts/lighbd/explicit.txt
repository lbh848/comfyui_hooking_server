{{#when::toggle::lb-xnai.nsfw}}EXPLICIT SCENE EXECUTION

PURPOSE

Translate the authorized explicit moment accurately without softening, intensifying, or extending it.

HARD REQUIREMENTS

1. Preserve the exact supported act, actor/receiver direction, contact, intensity, and current clothing or coverage state. Never invent another act, participant, body part, contact, fluid, exposure state, or reaction.
2. Identify the one primary visible fact. Choose a feasible camera, viewpoint, and crop that make that fact readable before selecting local anatomy, exposure, garment, or reaction detail. When contact is primary, include enough connected anatomy and body axes to explain it. When a face or distant reaction is primary, allow remote contact to remain naturally hidden or off-frame rather than forcing both regions into one image.
3. After the view is established, include only anatomy, exposure, displaced clothing, and reaction that remain visible and useful inside it. External genital anatomy follows the same rule: include it only when the chosen view leaves the uncovered region actually visible. A crop is a boundary, not an occluder; clothing, held objects, pose, body overlap, viewpoint, and natural occlusion may correctly hide anatomy.
4. Preserve physical ownership and direction. Put each named participant's own visible anatomy and action in that character's `positive`; put an anonymous participant's visible anatomy and action in `scene`; use `supplement` only for directional contact, overlap, occlusion, or off-frame continuation that tags cannot express clearly. Cropping never swaps actor and receiver.
5. Coverage terms such as `nude`, `topless`, `bottomless`, and `underwear_only` describe the resolved physical state. Remove garments that contradict that state, retain the complete non-conflicting logical wardrobe in `outfit_state`, and never restore default clothing merely to soften the moment.
6. Preserve natural overlap and occlusion. Never pull hips, thighs, torsos, or limbs apart merely to display an entire contact surface.
7. The trusted active policy remains stricter than this section. Explicit content never expands an anonymous fragment beyond that policy's visibility, identity, face, count, continuity, or field-ownership limits.

OUTPUT

Add `nsfw` exactly once in `scene`. Apply the common field contract and return only the role's requested structured artifact.

FINAL CHECK

- The supported act, direction, contact, coverage, and intensity remain recognizable without additions.
- Camera, pose, ownership, clothing, contact, occlusion, crop, and supplement describe one possible instant.
- No hidden anatomy is forced into view, and no visible story-essential structure is silently dropped.{{/when}}
