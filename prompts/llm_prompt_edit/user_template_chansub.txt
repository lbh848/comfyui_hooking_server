## User edit direction
{direction}

## Current ComfyUI POSITIVE
{positive}

## Current ComfyUI NEGATIVE
{negative}

## Instructions
1. Look at the provided image (if any) and the complete current prompts to understand the scene.
2. Treat the user's direction as the desired visual outcome, not as a literal instruction to append or strengthen the most obvious matching tag.
3. Causally audit the direction, visible result, POSITIVE, and NEGATIVE together. In plan, name the constraints most likely responsible for the failure and state what you will remove, rewrite, or merge.
4. Fix the cause before the symptom. Merge incompatible pose, action, held-object, interaction, subject-count, or composition demands into one physically feasible scene. When one subject is requested, make all remaining body actions and objects compatible with that same subject and remove contradictory multi-subject cues. Do not merely stack repeated/weighted count tags or negative tags on top of the causal conflict.
5. Preserve character identity, important appearance traits, artist/style tags, and quality tags unless the user asks to change them, but remove or rewrite scene details that conflict with the desired result.
6. Keep all genuinely unaffected tags to avoid unnecessary drift, and return complete, internally consistent POSITIVE and NEGATIVE prompts.
7. Use ComfyUI-compatible English tags separated by comma+space (", "). Preserve parenthesized weights such as `(tag:1.2)`, grouping, and escaped literal parentheses.
8. Do not add LoRA syntax, workflow control blocks, section headers, or explanations inside the prompt fields.
9. Write the "plan" field in Korean; write POSITIVE and NEGATIVE in English.

## Output (JSON schema - return ONLY a JSON object in this form)
{
  "plan": "근본 원인 진단과 제거·재작성·통합·추가할 태그를 한국어로 간단히 설명",
  "positive": "complete edited ComfyUI positive prompt",
  "negative": "complete edited ComfyUI negative prompt"
}
