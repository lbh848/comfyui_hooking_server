## User edit direction
{direction}

## Current scene tags (setup / background)
{setup}

## Current scene tags (char / character + pose + scene)
{char}

## Current scene tags (supplement / extra)
{supplement}

## Instructions
1. Look at the provided image (if any) and the tags above to understand the current scene.
2. Treat the user's direction as the desired visual outcome, not as a literal instruction to append or strengthen the most obvious matching tag.
3. Before rewriting, causally audit the direction, visible result, and whole current scene. In plan, name the prompt constraints most likely responsible for the unwanted result and state which of them you will remove, rewrite, or merge.
4. Fix the cause before treating the symptom. Merge incompatible pose, action, held-object, interaction, subject-count, or composition demands into one unambiguous and physically feasible scene. When one subject is requested, ensure every remaining pose, limb use, held object, and interaction can belong to that same subject, removing contradictory multi-subject cues. Do not merely stack repeated/weighted count tags or negatives on top of the conflict.
5. This is a V1 prompt where `char` is the WHOLE scene (background + character + pose + expression + outfit + auxiliary tags combined). Put the ENTIRE edited scene into `scene_char`. Leave `scene_setup` and `scene_supplement` as empty strings (""). Do NOT move tags between fields.
6. Preserve the character's core identity (the character/series name tag at the start of scene_char, e.g. "shifty \(nikke\)"); focus changes on scene/mood/pose/outfit.
7. Keep original tags for any part that does not need changing, but remove or rewrite every tag that conflicts with the desired result. Return one complete, internally consistent scene.
8. Keep using English danbooru-style tags separated by comma+space (", ").
9. Write the "plan" field in Korean; write scene_* tags in English.

## Output (JSON schema - return ONLY a JSON object in this form)
{
  "plan": "root-cause diagnosis and the tags/details to remove, rewrite, merge, or add (write in Korean)",
  "scene_setup": "background/location/lighting/weather/mood tags, comma+space separated",
  "scene_char": "character appearance/pose/expression/outfit tags, comma+space separated",
  "scene_supplement": "extra auxiliary tags, or empty string \"\""
}
